{
	"name": "@believe-x/ai-analysis",
	"version": "1.0.0",
	"description": "AI Analysis Service for Believe X AI Trading Bot",
	"scripts": {
		"dev": "uvicorn app:app --reload --host 0.0.0.0 --port 8000",
		"start": "uvicorn app:app --host 0.0.0.0 --port 8000",
		"lint": "pylint *.py",
		"test": "pytest"
	}
}
